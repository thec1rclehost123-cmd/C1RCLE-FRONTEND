import PageClient from './PageClient';

export const metadata = {
  title: 'Business Verification | Partner Dashboard',
  description: 'Manage document verification and bank setup',
};

export default function Page() {
  return <PageClient />;
}
